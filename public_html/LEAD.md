# LEAD.md — Инструкция Тимлида (haramain.online)

## 1. ОБЯЗАННОСТИ ТИМЛИДА
* Настройка базы данных MySQL (категории, валюты SAR, статусы заказов).
* Контроль целостности ядра OpenCart и защита папки `system/`.
* Ревью PR от разработчиков DEV_A и DEV_B.
* Развертывание изменений на боевом сервере `/home/u304509861/domains/haramain.online/public_html/`.

---

## 2. ЧЕК-ЛИСТ «СКЕЛЕТ ГОТОВ» (Выполнить до старта задач)
- [ ] В БД `oc_currency` добавлена валюта SAR (Код: `SAR`, Символ справа: `риал`, Значение: `1.00000000`), установлена по умолчанию.
- [ ] В БД `oc_category` и `oc_category_description` внесено дерево категорий (согласно `tech.md`).
- [ ] В БД созданы статусы заказов: "Ожидает оплаты", "Проверка чека", "В обработке", "Отменен".
- [ ] Очищены логи в `system/storage/logs/`.
- [ ] Проведена резервная копия БД и файлов витрины.

---

## 3. УПРАВЛЕНИЕ КОНТРАКТАМИ (CONTRACT GAP)
Если разработчик запрашивает новое поле в БД (например, поле для загрузки чека `receipt_file` в заказе):
1. Выполните SQL-миграцию в БД (например, `ALTER TABLE oc_order ADD COLUMN receipt_file VARCHAR(255) DEFAULT NULL;`).
2. Обновите версию ядра в `tech.md` (`v1.1.0`).
3. Сообщите разработчику о готовности структуры.

---

## 4. ЧЕК-ЛИСТ РЕВЬЮ PR
- [ ] Отсутствуют правки в `system/` и `config.php`.
- [ ] Все пути к изображениям и стилям корректны (`css/style.css`, `image/catalog/...`).
- [ ] В коде нет устаревших упоминаний "FarmShopping", "Турция", "Игр в категории".
- [ ] Код PHP не вызывает Fatal Error на PHP 8.x.