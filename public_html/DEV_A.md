# DEV_A.md — Инструкция Разработчика A

> **Зона ответственности:** Ребрендинг витрины, шапка/футер, меню категорий, селектор Мекка/Медина, кабинет продавца (`catalog/controller/add/product.php` и `catalog/controller/seller/*`).

---

## 1. ПРАВИЛА СТАРТА
* Прочитай `tech.md` (v1.0.0). Подчиняйся ему дословно.
* Если не хватает данных или схемы БД — выдай блок `CONTRACT GAP` и жди ответа Тимлида.

---

## 2. СПИСОК ЗАДАЧ (Выполнять строго по очереди, 1 PR = 1 задача)

### Задача A1: Ребрендинг и замена логотипа/текстов
* **Файлы:** `catalog/view/theme/default/template/common/header.tpl`, `footer.tpl`, `common/home.tpl`.
* **Что сделать:** 
  1. Заменить название площадки на `haramain.online`.
  2. Исправить опечатки ("из Туркции" -> "Мекка и Медина").
  3. Удалить упоминания "Игр в категории", "add_game_btn", "FarmShopping".
  4. Обновить копирайт в футере: `© 2026 haramain.online`.

### Задача A2: Вывод нового дерева категорий
* **Файлы:** `catalog/controller/common/header.php`, `catalog/view/theme/default/template/common/header.tpl`, `footer.tpl`.
* **Что сделать:** 
  1. Настроить выборку категорий из модели `catalog/category` согласно структуре из `tech.md`.
  2. Внедрить иконки категорий через SVG-спрайт из `footer.tpl`.
  3. Оформить мобильное меню категорий.

### Задача A3: Глобальный селектор города (Мекка / Медина)
* **Файлы:** `catalog/controller/common/header.php`, `header.tpl`, `catalog/view/theme/default/template/checkout/custom/shipping.tpl`.
* **Что сделать:**
  1. Добавить в шапку переключатель города: `Мекка` / `Медина`.
  2. Сохранять выбранный город в сессию (`$this->session->data['city']`).
  3. Автоматически подставлять выбранный город в оформление заказа и блокировать выбор других городов.

### Задача A4: Настройка формы добавления товара продавцом
* **Файлы:** `catalog/controller/add/product.php`, `catalog/view/theme/default/template/add/product_form.tpl`.
* **Что сделать:**
  1. Убедиться, что при добавлении товара продавцом привязываются новые категории.
  2. Валюта ввода цены — риалы (SAR).
  3. Добавить обязательный выбор города нахождения товара (Мекка или Медина).